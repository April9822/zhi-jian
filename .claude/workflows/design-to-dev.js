export const meta = {
  name: '知间全流程设计开发工作流',
  description: '知间主工作流：架构设计→产品设计→UX设计→安全审查→并行开发→QA测试。先设计后编码，April在每个阶段结束时敲板。',
  phases: [
    { title: '架构设计', detail: '系统架构师输出完整架构文档' },
    { title: '产品设计', detail: '产品设计师输出信息架构和页面流程' },
    { title: 'UX设计', detail: 'UX设计师输出交互规范和视觉规范' },
    { title: '安全审查', detail: '安全隐私审查独立审计设计产出' },
    { title: '并行开发', detail: 'AI工程师+前端工程师并行实现' },
    { title: 'QA测试', detail: 'QA测试员多场景回归测试' },
  ],
}

// ============================================================
// 知间 InBetween · 全流程设计开发工作流
//
// 使用方式：
//   完整流程: 让系统架构师启动，走完全部6个阶段
//   单阶段: 指定某个智能体名，只走该阶段
//
// 核心原则：
//   - 先设计后编码，绝不跳过设计直接写代码
//   - 每个阶段结束 → April 确认 → 下一阶段
//   - 安全审查拥有独立一票否决权
// ============================================================

// === 阶段1：架构设计 ===
phase('架构设计')

log('🏗️ 阶段1：系统架构师开始架构设计...')
log('')

const architecture = await agent(`
  作为知间AI系统架构师，请基于知间产品核心原则和已有研究文档，输出「知间关系操作系统 · 架构设计文档」。

  请覆盖以下内容：

  ## 1. 产品战略定位
  - 知间从V1.0「冲突解剖师」升级到「关系操作系统」的核心变化
  - 新的产品定位声明（一句话）
  - 与竞品的本质差异

  ## 2. 6阶段关系旅程架构
  - 每个阶段的用户心理状态
  - 每个阶段的核心功能需求
  - 阶段间的过渡逻辑

  ## 3. 页面模块体系
  - 个人层6模块的完整定义
  - 关系层5维度+情境回顾的完整定义
  - 模块间的信息层级关系

  ## 4. AI分析管线架构
  - 6步分析流程的详细设计
  - 每一步的输入/输出/约束
  - Prompt链的架构方案

  ## 5. 技术架构方案
  - 前端架构（页面结构+组件树）
  - 后端架构（API设计+数据流）
  - 部署架构（Vercel配置）

  ## 6. 版本演进路径
  - V1.0 → V1.1 → V2.0 的功能拆解
  - 每个版本的核心交付物

  ## 7. 待April确认的关键决策
  - 列出需要你敲板的关键选项，给出推荐方案

  注意：
  - 用中文输出
  - 不违反知间宪法三大禁止
  - 不着急写代码，先设计清楚
`, { label: '架构设计文档', phase: '架构设计' })

log('📐 架构设计文档已生成。')
log('👉 请 April 审阅架构设计，确认后进入阶段2。')

// 在实际运行中，这里会暂停等待April确认
// 工作流支持分阶段执行

// === 阶段2：产品设计 ===
phase('产品设计')

log('📐 阶段2：产品设计师开始信息架构设计...')

const productDesign = await agent(`
  作为知间产品设计师，请基于系统架构师的架构文档，输出「知间产品设计文档」。

  架构文档内容：
  ${architecture}

  请输出：

  ## 1. 完整页面清单
  - 列出所有页面（Welcome / 输入 / 等待建模 / 概览 / 个人卡 / 关系分析 / 情境重演 / 关系花园）
  - 每个页面的用户心理状态和设计目标

  ## 2. 个人层6模块详细设计
  - 每个模块的名称、内容结构、呈现方式
  - "从事实到成长"的排列逻辑

  ## 3. 关系层5维度+情境回顾详细设计
  - 每个维度的呈现方式（非数字评分）
  - 情境回顾的剧本式叙事结构

  ## 4. 情境重演交互流程
  - 用户怎么进入、怎么参与、怎么完成
  - 关键节点的交互决策

  ## 5. 关系花园的设计方案
  - 养分体系：信任/耐心/理解/包容怎么映射到视觉
  - 长期积累的成长逻辑

  ## 6. 用户完整动线图
  - 从Welcome到花园的每一步
  - 每一步的动机和引导

  注意：只设计信息架构和流程，不涉及颜色/字体/动效（留给UX设计师）
`, { label: '产品设计文档', phase: '产品设计' })

log('📐 产品设计文档已生成。')
log('👉 请 April 审阅产品设计，确认后进入阶段3。')

// === 阶段3：UX设计 ===
phase('UX设计')

log('🎨 阶段3：UX设计师开始交互和视觉设计...')

const uxDesign = await agent(`
  作为知间UX设计师，请基于产品设计文档，输出「知间UX设计规范」。

  产品设计文档：
  ${productDesign}

  请输出：

  ## 1. 每个页面的交互规范
  - 用户能做什么、怎么操作
  - 转场动画方案（消融/滑动/淡入）
  - 反馈机制（等待/成功/失败）

  ## 2. 组件级视觉规范
  - 每个组件使用的颜色（严格对照DESIGN-COLOR-SYSTEM）
  - 字体/字重/字号
  - 间距/圆角/阴影

  ## 3. 完整动效规范
  - Welcome动画（延续V10规范）
  - 建模等待动画
  - 报告分层揭示动画
  - 情境重演的暂停/继续动画
  - 关系花园的生长动画
  - 缓动曲线和时长参数

  ## 4. 状态覆盖
  - 每个交互元素的所有状态（默认/悬停/点击/禁用/加载/空）

  ## 5. 响应式适配方案
  - 断点定义
  - 各断点下的布局变化
  - 移动端特殊处理

  ## 6. 色彩合规自检
  - 对照DESIGN-COLOR-SYSTEM逐项检查
  - 标注是否出现禁止色彩

  注意：
  - 必须使用DESIGN-COLOR-SYSTEM中的色值
  - 移动端优先
  - 智者的调性——温暖不冰冷
`, { label: 'UX设计规范', phase: 'UX设计' })

log('🎨 UX设计规范已生成。')
log('👉 请 April 审阅UX设计，确认后进入阶段4。')

// === 阶段4：安全审查 ===
phase('安全审查')

log('🛡️ 阶段4：安全隐私审查独立审计...')

const securityAudit = await agent(`
  作为知间安全隐私审查，请对前面的设计产出进行独立安全审计。

  审查对象：
  - 架构设计：${JSON.stringify(architecture).substring(0, 500)}...
  - 产品设计：${JSON.stringify(productDesign).substring(0, 500)}...
  - UX设计：${JSON.stringify(uxDesign).substring(0, 500)}...

  请按照你的审查流程，逐项输出「安全隐私审查报告」：

  ## 隐私架构
  - 数据存储：评估
  - 传输加密：评估
  - 第三方共享：评估

  ## 8项风险护栏逐项审核
  - R1 谄媚偏袒：当前设计是否能防止？
  - R2 人格标签：是否有硬约束？
  - R3 三角化：是否指向直接沟通？
  - R4 隐私恐惧：是否有隐私承诺展示？
  - R5 医疗化：建议措辞是否安全？
  - R6 文化误读：是否覆盖中文特有信号？
  - R7 话术误用：是否有风格选择？
  - R8 质量不一致：是否有输入验证？

  ## 禁止词汇扫描
  - 设计文档中是否出现禁止词汇？

  ## 综合裁决
  ✅ 放行 / ⚠️ 有条件放行 / 🔴 阻断

  注意：
  - 你独立审查，不参与设计
  - 如果发现风险，明确指出修复要求
  - 不要因为赶进度而降低标准
`, { label: '安全审查报告', phase: '安全审查' })

log('🛡️ 安全审查完成。')
log('👉 如果裁决是🔴阻断，设计需要回到对应阶段修改。')
log('👉 请 April 审阅安全审查报告，确认后进入阶段5（开发）。')

// === 阶段5：并行开发 ===
phase('并行开发')

log('🔧 阶段5：AI工程师和前端工程师并行开发...')
log('')

const devResults = await parallel([
  // AI工程师
  () => agent(`
    作为知间AI工程师，现在开始实现AI分析管线。

    请基于以下设计文档实现：
    - 架构设计：${JSON.stringify(architecture).substring(0, 500)}...
    - 安全审查要求：${JSON.stringify(securityAudit).substring(0, 500)}...

    请输出：

    ## 1. 完整的System Prompt模板
    包括7个阶段的完整Prompt（角色识别/关系推断/时间线/情绪曲线/个人分析/关系分析/建议生成）

    ## 2. Few-shot示例库
    至少10对中文冲突样本

    ## 3. 中文冲突信号词典
    完整的中文特有表达→语境含义映射表

    ## 4. api/analyze.js 的完整代码
    Vercel Serverless Function，包含完整的Prompt链调用

    ## 5. 禁止词汇过滤函数
    在输出末尾执行的关键词扫描

    注意：
    - 所有Prompt用中文编写
    - 每个阶段包含System Prompt + Few-shot + 输出格式约束
    - 包含双侧分析平衡检查
    - 禁止词汇库必须硬编码到过滤函数中
  `, { label: 'AI引擎实现', phase: '并行开发' }),

  // 前端工程师
  () => agent(`
    作为知间前端工程师，现在开始实现前端页面。

    请基于以下设计规范实现：
    - UX设计规范：${JSON.stringify(uxDesign).substring(0, 500)}...
    - 设计系统守护规则
    - 代码质量规范
    - Welcome V10动画规范

    请按优先级创建以下文件：

    ## 1. index.html（Welcome首页）
    - 实现V10 24秒动画
    - 六段情绪色彩时间轴
    - 关键词特效
    - "开始"按钮

    ## 2. src/style.css（全局样式）
    - CSS自定义属性
    - 全局reset
    - 共享组件样式（Button/Card/曲线）

    ## 3. src/main.js（全局逻辑）
    - 页面状态管理
    - 页面路由/切换

    ## 4. src/input.js（输入页）
    - 文本输入+身份标注+提交

    ## 5. src/report.js（报告页）
    - 分层揭示逻辑
    - 个人卡6模块渲染
    - 关系5维度渲染
    - 情境回顾渲染

    ## 6. src/garden.js（关系花园）
    - 花园可视化（纯CSS）
    - 养分积累动画

    注意：
    - 移动端优先
    - 不使用任何框架
    - 不使用JS绘图库
    - HTML完全自包含
    - 注释用中文
  `, { label: '前端实现', phase: '并行开发' }),
])

log('🔧 开发完成。')
log('👉 进入阶段6（QA测试）。')

// === 阶段6：QA测试 ===
phase('QA测试')

log('🧪 阶段6：QA测试员开始多场景测试...')

const testResults = await agent(`
  作为知间QA测试员，请对开发产出进行完整测试。

  AI工程师产出：${JSON.stringify(devResults[0]).substring(0, 500)}...
  前端工程师产出：${JSON.stringify(devResults[1]).substring(0, 500)}...

  请输出「QA测试报告」：

  ## 1. 4个标准场景测试结果
  | 场景 | 结果 | 发现问题 |
  | 情侣家务 | | |
  | 职场上下级 | | |
  | 亲子催婚 | | |
  | 边界测试 | | |

  ## 2. 禁止词汇扫描
  - 扫描所有AI输出和页面文案
  - 报告命中项

  ## 3. 前端功能测试
  - 完整链路测试
  - 响应式测试
  - 触控目标检查

  ## 4. 回归测试
  - 与之前版本对比

  ## 5. 综合评估
  ✅ 可上线 / ⚠️ 有已知问题 / 🔴 阻塞上线

  ## 6. 已知剩余问题清单
`, { label: 'QA测试报告', phase: 'QA测试' })

log('')
log('='.repeat(60))
log('🎉 知间全流程设计开发工作流完成')
log('='.repeat(60))
log('')
log('📋 产出清单：')
log('  1. 架构设计文档')
log('  2. 产品设计文档')
log('  3. UX设计规范')
log('  4. 安全隐私审查报告')
log('  5. AI引擎实现')
log('  6. 前端页面实现')
log('  7. QA测试报告')
log('')
log('👉 April 最终敲板：审阅全部产出，决定是否部署上线。')

return {
  architecture,
  productDesign,
  uxDesign,
  securityAudit,
  devResults: devResults.filter(Boolean),
  testResults,
  status: 'complete',
}
